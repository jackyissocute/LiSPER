# SnapGene .dna Export

The annotated GenBank files in `../genbank/` are SnapGene-compatible and can be opened directly in SnapGene. Export native `.dna` files from SnapGene if a vendor specifically requires binary SnapGene format.
