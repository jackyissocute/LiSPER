# NaCl CHARMM-GUI inputs

The directories under `remote_results/systems/` are incomplete historical
source fragments, not eight usable CHARMM-GUI systems. `LiA3-Ref` retains
metadata showing a 0.15 M NaCl Solution Builder setting, but none of the eight
candidates currently has a complete audited NaCl export. All eight original
packages must be restored before paired production.
