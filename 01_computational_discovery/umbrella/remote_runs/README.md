# Umbrella remote runs

Scaffold for campaign logs and lean status syncs. The eight paired campaigns are complete.

Campaign record: `../remote_runs_umbrella_sampling_status.md`.
Fat runtime stays on the compute host under `/data/LiSPER_remote/` (and optional Jacky cold disk).
